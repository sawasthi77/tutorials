python is case sensitive language.
indentation is required in python.
no semicolon is required to differentaite two statements.
variable name can only contain letters, number and underscores.
Variable names can't start with numbers
variable names are case sensitive.
Reserved words can't be used.
variables names should be chosen which has some meaning.
If you ever want to modify the value stored in a global variable from in a function, you must use a global statement on that variable.
A method is the same thing as a function, except it is “called on” a value
